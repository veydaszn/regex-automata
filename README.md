# 🧠 Regex Automata Simulator (Python)
A simple but powerful Automata Theory project implemented in Python, demonstrating how regular expressions can be converted into NFAs and DFAs, and how strings can be tested for acceptance.

🚀 Features

Build NFAs from simple regular expressions (supports a|b, ab, a*)

Convert NFA → DFA

Check if a string is accepted or rejected

Clean Python code with comments

Easy to extend for more regex operators


📂 Project Structure
regex-automata/
│── src/
│ ├── nfa.py
│ ├── dfa.py
│ ├── regex_to_nfa.py
│ └── main.py
│
└── README.md


## 🏗 How to Run
 1️⃣ Clone the repo
```bash
git clone https://github.com/veydaszn/regex-automata
cd regex-automata/src
2️⃣ Run the main program
    python3 main.py
3️⃣ Expected Output

You’ll see:
Accepted? True/False

📚 Requirements
Python 3.8+
